/**
 * Pair keyword highlights only (kept for tests / reuse).
 * Prefer src/highlights.js for the registered provider.
 */
const { documentHighlightProvider, highlightsFor } = require("./highlights");

module.exports = { documentHighlightProvider, highlightsFor };
